{
  "name": "gdg-webdev-platform",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build-packages": "npm run build --workspace=@packages/api-typing --workspace=@packages/webdev-api-contracts --workspace=@packages/nexus-api-contracts --workspace=@packages/identity-api-contracts",
    "postinstall": "npm run build-packages"
  }
}
