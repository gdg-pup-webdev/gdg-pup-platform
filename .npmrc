# Use the strict, isolated symlink structure
node-linker=isolated

# Prevent packages from seeing "hidden" dependencies (no flat node_modules)
shamefully-hoist=false

# Require exact peer dependency satisfaction
strict-peer-dependencies=true

# Don't automatically link workspace projects unless version is "workspace:*"
link-workspace-packages=false