/**
 * ID Feature Module
 *
 * Exports all components needed to render the /id landing page shell.
 * Note: The dynamic /id/[id] page uses the user-profile feature.
 */

export { IdSection } from "./components/IdSection";
