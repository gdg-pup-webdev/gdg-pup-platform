
utility functions can either return the expected result or throw an error.
utilities must not be aware of the context of itself being called. by that, its use must not be restricted to a specific domain, but extensible to other because it does a process to a data, not to a specific resource. 

